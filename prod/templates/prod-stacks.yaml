Resources:
  GlueJob:
    Type: AWS::Glue::Job
    Properties:
      Role: arn:aws:iam::470384041006:role/prod-glue-role
      Command:
        Name: prod-etl-job
        ScriptLocation: s3://prod-bucket-tl/scripts/etl_job_prod.py
      DefaultArguments:
        "--job-bookmark-option": "job-bookmark-enable"
      Connections:
        Connections:
          - prod-redshift-connection
      ExecutionProperty:
        MaxConcurrentRuns: 1
